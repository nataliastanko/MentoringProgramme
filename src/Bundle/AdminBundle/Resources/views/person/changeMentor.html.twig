{% extends 'UserBundle::account.html.twig' %}

{% block body -%}
    <h1>{{ 'mentee.changeMentor.button' | trans }}</h1>
    <h3>{{ 'mentee.self' | trans }}:&nbsp;{{ person.fullname }}</h3>

    <div class="row">
        <div class="col-sm-6 col-xs-12">
            {{ form_start(edit_form) }}
                {{ form_widget(edit_form) }}

                <button type="submit" class="btn btn-default" id="_submit" name="_submit">{{ 'button.save' | trans }}</button>

            {{ form_end(edit_form) }}
        </div>
    </div>

{% endblock %}
