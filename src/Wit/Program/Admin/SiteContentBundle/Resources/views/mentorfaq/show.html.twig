{% extends 'WitProgramAccountUserBundle::account.html.twig' %}

{% block body %}
    {# <ol class="breadcrumb">
      <li>{{- 'menu.sitecontent' | trans }}</li>
      <li><a href="#">{{ 'mentorfaq.list' | trans }}</a></li>
      <li class="active">{{ 'mentorfaq.detail' | trans }}</li>
    </ol>
 #}
    <div class="page-header">
        <h1>{{ 'mentorfaq.detail' | trans }}</h1>
    </div>

    <ul class="nav-internal list-unstyled">
        <li>
            <a href="{{ path('mentorfaq_edit', { 'id': mentorfaq.id }) }}" class="btn btn-primary btn-sm">{{ 'button.edit' | trans }}</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input class="btn btn-primary btn-sm" type="submit" value="{{ 'button.delete' | trans }}">
            {{ form_end(delete_form) }}
        </li>
    </ul>

    <div class="row">
        <div class="col-md-6 col-sm-12">

            <table class="table table-hover">
                <tbody>
                    <tr>
                        <th>{{ 'faq.question' | trans }}</th>
                        <td>{{ mentorfaq.question }}</td>
                    </tr>
                    <tr>
                        <th>{{ 'faq.answer' | trans }}</th>
                        <td>{{ mentorfaq.answer }}</td>
                    </tr>
                </tbody>
            </table>
        </div>
    </div>

{% endblock %}
